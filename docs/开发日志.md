# 开发日志

## 0.118.0.0 - 2026-05-28

- 🧩[开发]-`UnifiedDiffViewer` 新增 `IsSideBySide`、`SplitLines` 和 `UnifiedDiffSplitLine`，同一份 Git diff 可渲染为 unified 或 side-by-side 布局。
- 🧩[开发]-新增 `UnifiedDiffSideLinePresenter`，按左右两栏分别显示旧/新行号、前缀和内容，并复用新增、删除、上下文和 hunk 主题色。
- 🧩[开发]-`AppSettings` 新增 `ShowSideBySideDiff`，View 菜单 checkbox 切换并持久化到 `App.config`。
- 🌐[开发]-补充中英文菜单和状态提示资源，并同步 `AvaGithubDesktopL` 常量。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；中英文 JSON 资源一致；输出目录 `App.config` 写入 `ShowSideBySideDiff=True`；截图保存 `docs/screenshots/side-by-side-diff.png`。

## 0.117.0.0 - 2026-05-28

- 🔧[开发]-`RepositoryWorkspaceLoader`、`GitRepositoryService` 和主窗口初始化改为并行加载互不依赖的数据，首个 Diff 延迟 180ms 后加载，并对连续选择切换取消过期 Diff 请求。
- 🎨[开发]-字体回退扩展为 Inter、Noto/思源、微软雅黑、苹方和 Segoe UI 组合，Diff/日志使用 Cascadia、JetBrains、Noto Sans Mono 等等宽字体回退。
- 🎨[开发]-菜单资源继续封装在 `AvaGithubDesktop.Controls.Themes`，保留 `MenuFlyoutMaxHeight=Infinity`，并补齐 Semi 菜单资源键和各主题菜单颜色资源。
- 🎨[开发]-新增 `Windows.axaml` 覆盖 `CodeWFWindow` 标题栏下边框厚度，所有窗口标题栏使用 1px 分隔线增强层级。
- 🧩[开发]-`AppSettings` 新增 `HideWhitespaceChanges`，View 菜单通过 checkbox 切换并持久化；工作区和 History 文本 Diff 调用加入 `--ignore-all-space`。
- 📝[开发]-需求文档、更新日志和版本号同步本轮启动、菜单、主题和忽略空白 Diff 体验优化，并移除内置 Git 运行时计划项。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；中英文 JSON 资源一致；`git diff --check` 通过；截图保存 `startup-polish-theme-*.png`、`menu-view-popup-*.png`、`startup-polish-log-hidden.png`、`titlebar-bottom-border.png`、`menu-hide-whitespace-checked.png` 和 `diff-hide-whitespace-enabled.png`。

## 0.116.0.0 - 2026-05-28

- 🔧[开发]-新增 `BranchDialogFilter` 和 `BranchDialogFilterResult`，集中维护分支对话框的过滤匹配和选中项恢复。
- 🧩[开发]-Merge branch、Rebase branch 和 Set upstream 三个窗口 ViewModel 改为复用同一过滤 helper，保留各自首选项选择规则。
- 📝[开发]-更新日志和版本号同步本轮分支对话框职责拆分。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；`git diff --check` 通过。

## 0.115.0.0 - 2026-05-28

- 🔧[开发]-新增 `GitCommandRunner`，集中维护 Git 进程启动、stdout/stderr 读取、退出码处理和详细命令日志。
- 🧩[开发]-`GitRepositoryService` 的 required/optional/to-file Git 调用改为委托 runner，仓库服务保留命令编排职责。
- 📝[开发]-更新日志和版本号同步本轮 Git 执行职责拆分。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；`git diff --check` 通过；启动应用后 UIAutomation 确认当前仓库工作区仍可加载。

## 0.114.0.0 - 2026-05-28

- 🔎[开发]-`OperationLogView` 过滤输入框监听 `Esc`，存在过滤词时清空并重新渲染完整日志。
- 📝[开发]-更新日志和版本号同步本轮操作日志过滤清除体验。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；`git diff --check` 通过。

## 0.113.0.0 - 2026-05-28

- ⌨️[开发]-View 菜单新增 Focus operation log filter/聚焦操作日志过滤，快捷键为 `Ctrl+Shift+L`。
- 🧩[开发]-`OperationLogView` 暴露 `FocusFilter()`，主窗口在日志栏隐藏时先显示日志栏再聚焦过滤输入框。
- 🌐[开发]-补充中英文菜单资源和快捷键文档。
- 📝[开发]-需求文档、更新日志和版本号同步本轮日志过滤键盘导航。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；中英文 JSON 资源一致；`git diff --check` 通过；UIAutomation 确认操作日志过滤输入框“过滤日志”可见，并通过源码确认 View 菜单和 `Ctrl+Shift+L` 绑定到聚焦逻辑。

## 0.112.0.0 - 2026-05-28

- 🔎[开发]-`OperationLogView` 新增过滤文本属性，渲染时按日志级别和消息内容筛选。
- 🎨[开发]-操作日志主题模板在日志正文上方加入紧凑过滤框，样式继续封装在 `AvaGithubDesktop.Controls.Themes`。
- 🌐[开发]-补充中英文日志过滤占位文本，并在主窗口传入控件。
- 📝[开发]-需求文档、更新日志和版本号同步本轮操作日志过滤体验优化。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；中英文 JSON 资源一致；`git diff --check` 通过；UIAutomation 确认操作日志过滤输入框“过滤日志”可见。

## 0.111.0.0 - 2026-05-28

- 🧾[开发]-新增 `GitCommandLog`，统一格式化 `git -C "<repo>" ...` 命令文本并写入操作日志。
- 🔐[开发]-命令日志对 URL userinfo 和常见敏感查询参数脱敏，避免 token/password 随远端地址进入日志。
- 🧩[开发]-`GitRepositoryService.RunGitAsync`、`RunGitToFileAsync` 和最近仓库远端 URL 读取命令均记录开始、退出码和耗时。
- 📝[开发]-需求文档、更新日志和版本号同步本轮 Git 命令日志体验。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；`git diff --check` 通过；临时验证程序订阅 `Logger.ReadAllUiLogsAsync` 后调用仓库加载，确认 UI 日志流出现 `[git] $ git -C "D:\github\AvaGithubDesktop" rev-parse --show-toplevel` 和对应 exit 记录。

## 0.110.0.0 - 2026-05-28

- ⌨️[开发]-View 菜单参考 GitHub Desktop 新增 Expand active resizable 和 Contract active resizable。
- 🧩[开发]-`Ctrl+9` / `Ctrl+8` 在 Changes 页面调整主工作区左栏宽度，在 History 页面调整提交文件列表宽度，并立即写入布局配置。
- 🌐[开发]-补充中英文菜单资源和快捷键文档，并同步 `AvaGithubDesktopL` 常量。
- 📝[开发]-需求文档、更新日志和版本号同步本轮可调区域快捷键。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；中英文 JSON 资源一致；`git diff --check` 通过；源码确认 View 菜单和 `Ctrl+9` / `Ctrl+8` 绑定到当前可调区域调整逻辑。

## 0.109.0.0 - 2026-05-28

- 💾[开发]-`AppSettings` 新增 `WindowWidth` 与 `WindowHeight`，主窗口普通状态关闭时保存尺寸。
- 🧩[开发]-主窗口初始化时按配置恢复宽高，并保留 960x620 最小尺寸约束，避免异常配置导致窗口不可用。
- 📝[开发]-需求文档、更新日志和版本号同步本轮窗口尺寸恢复体验优化。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；`git diff --check` 通过；启动后关闭程序确认输出目录 `App.config` 写入 `WindowWidth` 与 `WindowHeight`。

## 0.108.0.0 - 2026-05-28

- 💾[开发]-`AppSettings` 新增 `WorkspaceSidebarWidth`、`HistoryFileListWidth` 和 `OperationLogHeight`，布局尺寸随 `App.config` 持久化。
- 🧩[开发]-主窗口启动时恢复用户上次调整过的工作区左栏、History 文件列表和操作日志高度，关闭窗口时保存当前尺寸。
- 🎛️[开发]-三个 `GridSplitter` 补齐水平/垂直拉伸对齐，确保可拖拽热区覆盖整条分隔线。
- 📝[开发]-需求文档、更新日志和版本号同步本轮布局持久化体验优化。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；`git diff --check` 通过；UIAutomation 确认主工作区和日志区 `GridSplitter` Thumb 仍可见；启动后关闭程序确认输出目录 `App.config` 写入三项布局尺寸。

## 0.107.0.0 - 2026-05-28

- 🎛️[开发]-主工作区在左侧文件/提交列表和右侧内容之间新增 `GridSplitter`，默认 360px 左栏可在 260px 到 560px 间调整，为 Diff 留出更多阅读空间。
- 🎛️[开发]-History 提交文件列表和 Diff 之间新增内部 `GridSplitter`，提交详情页同样可调整文件列表宽度。
- 🎛️[开发]-操作日志上沿新增横向 `GridSplitter`，日志行高在 View 层记录并在隐藏日志时折叠为 0，避免隐藏后留下空白区域。
- 🎨[开发]-分隔条样式封装在 `AvaGithubDesktop.Controls.Themes` 的 `Panels.axaml`，悬停时使用强调色反馈，不向 `App.axaml` 写样式。
- 📝[开发]-需求文档、更新日志和版本号同步本轮可拖拽布局体验优化。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；`git diff --check` 通过；UIAutomation 确认主工作区和日志区两个 `GridSplitter` Thumb 可见。当前 Windows 会话拒绝屏幕截图与剪贴板截图（`CopyFromScreen`/`BitBlt`/`ImageGrab`/`Alt+PrintScreen` 均失败），未提交无效黑图。

## 0.106.0.0 - 2026-05-28

- 🧭[开发]-Help 菜单参考 GitHub Desktop 补齐 Report issue、Contact GitHub support 和 Show User Guides 外部帮助入口。
- 🧩[开发]-`IHelpService` 复用 `RepositoryShellService.OpenUrlAsync` 跨平台打开帮助链接，Contact 支持带上当前应用版本参数。
- 🌐[开发]-补充 Help 外部链接菜单、状态提示和失败提示中英文资源，并同步 `AvaGithubDesktopL` 常量。
- 📝[开发]-需求文档、更新日志和版本号同步本轮 Help 外部链接入口。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；中英文 JSON 资源一致；`git diff --check` 通过；UIAutomation 展开 Help 菜单确认“报告问题...”“联系 GitHub 支持...”和“显示用户指南”入口可见，截图保存在 `docs/screenshots/help-external-links-menu.png`。

## 0.105.0.0 - 2026-05-28

- 🧭[开发]-View 菜单新增显示/隐藏 Stashed changes 入口，快捷键为 `Ctrl+H`。
- 🧩[开发]-Changes 的 Stashed changes 区块改为受 `IsStashedChangesVisible` 控制，默认显示，有 stash 时可通过菜单隐藏或恢复。
- 🌐[开发]-补充中英文菜单资源和快捷键说明文档。
- 📝[开发]-需求文档、更新日志和版本号同步本轮 View 菜单 Stashed changes 显隐入口。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；中英文 JSON 资源一致；`git diff --check` 通过；UIAutomation 展开 View 菜单确认“隐藏已贮藏的更改”入口可见，截图保存在 `docs/screenshots/view-toggle-stashed-changes-menu.png`。

## 0.104.0.0 - 2026-05-28

- 🧭[开发]-Repository 菜单新增“从列表移除”入口，快捷键为 `Ctrl+Backspace`，补齐 GitHub Desktop 的 Remove repository 菜单能力。
- 🧩[开发]-移除当前仓库记录时同步清空 `LastRepositoryPath`，避免下次启动又恢复已移除仓库。
- 📝[开发]-需求文档、更新日志、快捷键文档和版本号同步本轮 Repository 菜单入口。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；中英文 JSON 资源一致；`git diff --check` 通过；UIAutomation 展开 Repository 菜单确认“从列表移除”入口可见，截图保存在 `docs/screenshots/repository-remove-from-list-menu.png`。

## 0.103.0.0 - 2026-05-28

- 📦[开发]-参考 Vex 增加 `publish_all.bat` 和 `package_all.bat`，支持一键发布并打包 release 产物。
- 📦[开发]-新增 `win-x64`、`win-x86`、`linux-x64`、`linux-arm64`、`osx-x64` 和 `osx-arm64` 发布 profile。
- 📦[开发]-新增 `scripts/package_ava_github_desktop_artifacts.ps1`，生成 zip、sha256 校验文件和 release manifest。
- 🧩[开发]-项目 `RuntimeIdentifiers` 补齐 `win-x86`，发布目录统一输出到仓库根目录 `publish/<rid>`。
- 📝[开发]-需求文档、更新日志和版本号同步本轮发布打包脚本能力。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；`git diff --check` 通过；执行 `package_all.bat --force` 成功，生成 6 个发布目录、6 个 zip、6 个 sha256 和 `AvaGithubDesktop-v0.103.0.0-release-manifest.json`。

## 0.102.0.0 - 2026-05-28

- 🧭[开发]-顶层菜单顺序调整为 File、Edit、View、Repository、Branch、Help，对齐 GitHub Desktop Windows/Linux 默认菜单结构。
- 📝[开发]-需求文档、更新日志和版本号同步本轮菜单顺序对齐。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；`git diff --check` 通过；UIAutomation 确认顶层菜单顺序为“文件、编辑、查看、仓库、分支、帮助”，截图保存在 `docs/screenshots/top-menu-order.png`。

## 0.101.0.0 - 2026-05-28

- 🧭[开发]-新增 Edit 菜单，补齐 Undo、Redo、Cut、Copy、Paste、Select all 和 Find 常用入口。
- 🧩[开发]-主窗口记录最近聚焦的 TextBox，菜单点击导致焦点转移后仍能对原输入框执行文本编辑动作。
- ⌨️[开发]-新增 `Ctrl+F` 聚焦 Changes 文件过滤，保留既有 `Ctrl+L`。
- 🎨[开发]-在主题资源覆盖 `MenuFlyoutMaxHeight=Infinity`，取消菜单弹层最大高度限制。
- 🌐[开发]-补充中英文菜单资源和快捷键说明文档。
- 📝[开发]-需求文档、更新日志和版本号同步本轮菜单入口与弹层高度调整。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；中英文 JSON 资源一致；`git diff --check` 通过；UIAutomation 展开 Edit 菜单确认文本编辑入口可见，截图保存在 `docs/screenshots/edit-menu.png`。

## 0.100.0.0 - 2026-05-28

- 🧭[开发]-File 菜单新增 Exit/退出入口，并显示 `Alt+F4` 快捷键。
- 🧩[开发]-退出入口由主窗口关闭桥接处理，保持窗口生命周期逻辑集中在 View 层。
- 🌐[开发]-补充中英文菜单资源。
- 📝[开发]-需求文档、更新日志和版本号同步本轮 File 菜单退出入口。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；中英文 JSON 资源一致；`git diff --check` 通过；UIAutomation 展开 File 菜单确认“退出”入口可见，截图保存在 `docs/screenshots/file-exit-menu.png`。

## 0.99.0.0 - 2026-05-28

- 🧭[开发]-Help 菜单新增 Show logs in file manager/在文件管理器中显示日志入口，对齐 GitHub Desktop Help 菜单。
- 🧩[开发]-`IHelpService` 新增日志目录打开能力，复用 `CodeWF.Log.Core.Logger.LogDir` 并确保目录存在。
- 🌐[开发]-补充中英文菜单资源和状态提示。
- 📝[开发]-需求文档、更新日志和版本号同步本轮 Help 菜单日志入口。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；中英文 JSON 资源一致；`git diff --check` 通过；UIAutomation 展开 Help 菜单确认“在文件管理器中显示日志”入口可见，截图保存在 `docs/screenshots/help-show-logs-menu.png`。

## 0.98.0.0 - 2026-05-28

- ⌨️[开发]-View 菜单新增 Toggle full screen/切换全屏入口，快捷键为 `F11`。
- 🧩[开发]-主窗口记录进入全屏前的窗口状态，退出全屏时恢复普通或最大化状态。
- 🌐[开发]-补充中英文菜单资源和快捷键说明文档。
- 📝[开发]-需求文档、更新日志和版本号同步本轮 GitHub Desktop View 菜单全屏入口对齐。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；中英文 JSON 资源一致；`git diff --check` 通过；运行时 F11 验证进入 1920x1080 全屏并恢复 1220x780 窗口，截图保存在 `docs/screenshots/toggle-full-screen.png`。

## 0.97.0.0 - 2026-05-28

- ⌨️[开发]-View 菜单新增 Go to Summary/跳转到提交摘要入口，快捷键为 `Ctrl+G`。
- ⌨️[开发]-主窗口快捷键处理新增提交摘要聚焦路径，会先切回 Changes 页面再聚焦提交摘要输入框。
- 🐛[修复]-主窗口快捷键处理改为仅匹配纯 `Ctrl` 组合，避免拦截 `Ctrl+Shift+T/G` 等菜单快捷键。
- 🌐[开发]-补充中英文菜单资源和快捷键说明文档。
- 📝[开发]-需求文档、更新日志和版本号同步本轮 GitHub Desktop View 菜单快捷入口对齐。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；中英文 JSON 资源一致；`git diff --check` 通过；UIAutomation 展开 View 菜单确认“跳转到提交摘要”入口可见，截图保存在 `docs/screenshots/go-to-summary-menu.png`。

## 0.96.0.0 - 2026-05-28

- 🎨[开发]-Changes 底部提交区域改为主题资源驱动的输入框、amend 选项、stash 和 commit 按钮样式。
- 🎨[开发]-提交区域新增强调线、提交图标徽标以及 stash/commit 按钮图标，补齐 `OcticonGitCommit` 和 `OcticonArchive` 资源。
- 📝[开发]-需求文档、更新日志和版本号同步本轮提交区域主题化。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；Light、Dark、Aquatic、Desert、Dusk、NightSky 主题截图验证提交区域显示正常，截图保存在 `docs/screenshots/commit-area-themed-*.png`。

## 0.95.0.0 - 2026-05-28

- 🎨[开发]-Changes tab 数字徽标抽入 `repositoryTabCountBadge` 主题样式，使用强调色背景、边框和文字。
- 🎨[开发]-Changes/History tab 选中下划线抽入 `repositoryTabIndicator` 主题样式，减少主窗口 XAML 内联视觉参数。
- 📝[开发]-需求文档、更新日志和版本号同步本轮 tab 徽标主题化。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；Light/NightSky 主题截图验证 tab 数字徽标和下划线显示正常，截图保存在 `docs/screenshots/repository-tab-badge-*.png`。

## 0.94.0.0 - 2026-05-28

- 🎨[开发]-Changes 和 History 文件状态徽标改为 `fileStatusBadge` 主题样式，支持 added、warning、danger 语义 tone。
- 🧩[开发]-移除 `GitChangeItem` 和 `GitCommitFileItem` 模型中的徽标背景/前景十六进制颜色，模型只保留 Git 状态语义。
- 📝[开发]-需求文档、更新日志和版本号同步本轮文件状态徽标主题化。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；Light/Dark 主题截图验证状态徽标可读且随主题适配，截图保存在 `docs/screenshots/status-badges-themed-*.png`。

## 0.93.0.0 - 2026-05-28

- 🎨[开发]-Changes/History tab 选中态改为 `repositoryTab.selected` 样式类和动态主题资源控制。
- 🧩[开发]-移除 `MainWindowViewModel` 中 tab 背景/前景十六进制颜色属性，减少 ViewModel 对纯 UI 样式的职责耦合。
- 📝[开发]-需求文档、更新日志和版本号同步本轮 tab 主题适配和样式下沉。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；Light/Dark 主题截图验证 tab 选中态和下划线显示正常，截图保存在 `docs/screenshots/repository-tabs-themed-*.png`。

## 0.92.0.0 - 2026-05-28

- 🎨[开发]-工具栏分段按钮和工具栏语言选择控件默认背景改为透明，由父级工具栏承载连续科技蓝渐变。
- 🎨[开发]-保留分段边界和悬停背景反馈，避免每个工具栏分段重复绘制渐变导致色带割裂。
- 📝[开发]-需求文档、更新日志和版本号同步本轮工具栏视觉连贯性优化。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；已截图验证 Light 和 NightSky 主题工具栏连续渐变，截图保存在 `docs/screenshots/toolbar-continuous-*.png`。

## 0.91.0.0 - 2026-05-28

- 🎨[开发]-重做应用主题调色板，Light、Dark、Aquatic、Desert、Dusk 和 NightSky 都改为科技蓝主调，并补齐面板、Diff、日志、工具栏和状态栏资源。
- 🧩[开发]-新增 `AvaGithubDesktopThemeVariants`，自定义主题继承 Avalonia Light/Dark 基础变体，避免 Semi 半主题默认控件色导致黑块、黄边等不一致视觉。
- 🎨[开发]-登录、Markdown 文档和丢弃更改确认窗口标题栏改为动态主题资源，stash/操作提示条改为强调色资源。
- 📝[开发]-需求文档、更新日志和版本号同步本轮多主题视觉适配。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；已切换并截图验证 Light、Dark、Aquatic、Desert、Dusk、NightSky 主题，截图保存在 `docs/screenshots/theme-tech-blue-*.png`。

## 0.90.0.0 - 2026-05-28

- 🧩[开发]-History 创建标签流程复用 `RepositoryOperationCommandService`。
- 🧠[开发]-标签列表加载和创建标签弹窗仍由主窗口协调，实际创建标签的忙碌状态、状态消息和失败回填统一由命令运行服务处理。
- 🧠[开发]-`MainWindowViewModel` 移除创建标签流程中的重复 try/catch 忙碌状态管理，主 ViewModel 行数降至 3478 行。
- 📝[开发]-需求文档、更新日志和版本号同步本轮创建标签命令运行模板复用。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；需求文档禁用措辞扫描通过；`git diff --check` 通过；已生成 `docs/screenshots/create-tag-command-runner.png` 做基础界面回归截图验证。

## 0.89.0.0 - 2026-05-28

- 🧩[开发]-Stash all changes 复用 `RepositoryOperationCommandService`。
- 🧠[开发]-stash 完成消息通过延迟完成消息判断是否真正创建 stash，保留“没有可贮藏更改”的提示分支。
- 🧠[开发]-`MainWindowViewModel` 移除 stash all 中的重复 try/catch 忙碌状态管理，主 ViewModel 行数降至 3483 行。
- 📝[开发]-需求文档、更新日志和版本号同步本轮 stash all 命令运行模板复用。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；需求文档禁用措辞扫描通过；`git diff --check` 通过；已生成 `docs/screenshots/stash-all-command-runner.png` 做基础界面回归截图验证。

## 0.88.0.0 - 2026-05-28

- 🧩[开发]-Fetch、Fetch all、Pull、Push、Publish、Fetch LFS 和 Pull LFS 远端同步主流程复用 `RepositoryOperationCommandService`。
- 🧠[开发]-远端同步只保留 `RunRemoteGitOperationAsync` 负责具体 Git 命令分派，忙碌状态、状态消息、成功后 workspace loader 刷新和失败回填统一由命令运行服务处理。
- 🧠[开发]-`MainWindowViewModel` 移除远端同步主流程中的重复 try/catch 状态机，主 ViewModel 行数降至 3485 行。
- 📝[开发]-需求文档、更新日志和版本号同步本轮远端同步命令运行模板复用。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；需求文档禁用措辞扫描通过；`git diff --check` 通过；已生成 `docs/screenshots/remote-sync-command-runner.png` 做基础界面回归截图验证。

## 0.87.0.0 - 2026-05-28

- 🧩[开发]-`RepositoryOperationCommandRequest` 的完成消息改为延迟生成，支持根据 Git 操作结果决定完成文案。
- 🧠[开发]-Update from default branch、Merge branch、Squash merge branch 和 Rebase current branch 复用 `RepositoryOperationCommandService`，并保留冲突失败后尝试刷新工作区的行为。
- 🧠[开发]-动态 Already up-to-date/Success 文案从重复 try/catch 流程中剥离，主 ViewModel 行数降至 3495 行。
- 📝[开发]-需求文档、更新日志和版本号同步本轮 merge/rebase 命令运行模板复用。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；需求文档禁用措辞扫描通过；`git diff --check` 通过；已生成 `docs/screenshots/merge-rebase-command-runner.png` 做基础界面回归截图验证。

## 0.86.0.0 - 2026-05-28

- 🧩[开发]-Rename branch、Set upstream、Unset upstream、Delete branch 和 Set default branch 复用 `RepositoryOperationCommandService`。
- 🧠[开发]-分支维护操作的忙碌状态、状态消息、成功后 workspace loader 刷新和失败回填集中到统一命令运行模板。
- 🧠[开发]-`MainWindowViewModel` 移除分支维护操作中的重复 try/catch 流程，主 ViewModel 行数降至 3499 行。
- 📝[开发]-需求文档、更新日志和版本号同步本轮分支维护命令运行模板复用。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；需求文档禁用措辞扫描通过；`git diff --check` 通过；已生成 `docs/screenshots/branch-maintenance-command-runner.png` 做基础界面回归截图验证。

## 0.85.0.0 - 2026-05-28

- 🧩[开发]-Commit 和 Amend last commit 提交流程复用 `RepositoryOperationCommandService`。
- 🧠[开发]-成功提交后的摘要/描述清空、amend 状态复位和 workspace loader 刷新保持原有顺序，同时纳入统一命令运行模板。
- 🧠[开发]-`MainWindowViewModel` 移除提交流程中的重复 try/catch 忙碌状态管理，主 ViewModel 行数降至 3529 行。
- 📝[开发]-需求文档、更新日志和版本号同步本轮提交流程命令运行模板复用。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；需求文档禁用措辞扫描通过；`git diff --check` 通过；已生成 `docs/screenshots/commit-command-runner.png` 做基础界面回归截图验证。

## 0.84.0.0 - 2026-05-28

- 🧩[开发]-Create branch 和 Checkout branch 复用 `RepositoryOperationCommandService`。
- 🧠[开发]-创建分支成功后的过滤清空、工作区刷新和状态消息仍保持原有顺序，切换分支成功后统一走 workspace loader 刷新。
- 🧠[开发]-`MainWindowViewModel` 移除分支创建/切换中的重复 try/catch 命令流程，主 ViewModel 行数降至 3532 行。
- 📝[开发]-需求文档、更新日志和版本号同步本轮分支命令运行模板复用。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；需求文档禁用措辞扫描通过；`git diff --check` 通过；已生成 `docs/screenshots/branch-command-runner.png` 做基础界面回归截图验证。

## 0.83.0.0 - 2026-05-28

- 🧩[开发]-新增 `RepositoryWorkspaceState`、`IRepositoryWorkspaceLoader` 和 `RepositoryWorkspaceLoader`，集中加载仓库快照、分支列表和历史提交。
- 🧠[开发]-打开仓库、提交后刷新、同步后刷新、创建分支和切换分支统一调用 workspace loader，主窗口不再重复拼三段 Git 读取。
- 🧠[开发]-`MainWindowViewModel` 新增 `ApplyWorkspace` 作为唯一工作区应用入口，主 ViewModel 行数降至 3543 行。
- 📝[开发]-需求文档、更新日志和版本号同步本轮仓库工作区加载服务化拆分。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；需求文档禁用措辞扫描通过；`git diff --check` 通过；已生成 `docs/screenshots/repository-workspace-loader.png` 做基础界面回归截图验证。

## 0.82.0.0 - 2026-05-28

- 🧩[开发]-Restore stash、Discard stash、Push tags 和 Update submodules 复用 `RepositoryOperationCommandService`。
- 🧠[开发]-新增同步操作忙碌状态设置 helper，Push tags 和 Update submodules 的 `_activeSyncOperation` 与 `IsSyncing` 状态由同一入口维护。
- 🧠[开发]-`MainWindowViewModel` 移除 stash 和部分同步入口的重复 try/catch 命令流程，主 ViewModel 行数降至 3558 行。
- 📝[开发]-需求文档、更新日志和版本号同步本轮 stash/同步命令运行模板复用。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；需求文档禁用措辞扫描通过；`git diff --check` 通过；已生成 `docs/screenshots/stash-sync-command-runner.png` 做基础界面回归截图验证。

## 0.81.0.0 - 2026-05-28

- 🧩[开发]-Changes 标记冲突已解决和丢弃更改流程复用 `RepositoryOperationCommandService`。
- 🧠[开发]-保留确认取消、失败消息和失败后不额外刷新工作区的既有行为，同时统一忙碌状态、状态栏消息和成功后刷新模板。
- 🧠[开发]-`MainWindowViewModel` 移除 Changes 操作中的重复 try/catch 命令流程，主 ViewModel 行数降至 3575 行。
- 📝[开发]-需求文档、更新日志和版本号同步本轮 Changes 命令运行模板复用。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；需求文档禁用措辞扫描通过；`git diff --check` 通过；已生成 `docs/screenshots/changes-operation-command-runner.png` 做基础界面回归截图验证。

## 0.80.0.0 - 2026-05-28

- 🧩[开发]-`RepositoryOperationCommandRequest` 改为接收已格式化的开始、完成和失败消息生成器，命令运行服务不再绑定资源 key 形态。
- 🧠[开发]-History 选中提交 Revert 和 Cherry-pick 复用 `RepositoryOperationCommandService`，统一忙碌状态、状态栏消息、工作区刷新和失败回填模板。
- 🧠[开发]-`MainWindowViewModel` 移除两套重复 try/catch 命令流程，主 ViewModel 行数降至 3583 行。
- 📝[开发]-需求文档、更新日志和版本号同步本轮提交操作命令运行模板复用。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；需求文档禁用措辞扫描通过；`git diff --check` 通过；已生成 `docs/screenshots/repository-operation-message-runner.png` 做基础界面回归截图验证。

## 0.79.0.0 - 2026-05-28

- 🧩[开发]-`GitRepositoryOutputParser` 继续接管 `status --porcelain` 变更文件解析和 `log --name-status` 历史提交解析。
- 🧠[开发]-Git 路径 quotePath 解码、rename/copy 路径还原和空提交兜底模型集中在输出解析器中复用。
- 🧠[开发]-`GitRepositoryService` 移除 status/history 解析 helper，行数降至 1484 行；`GitRepositoryOutputParser` 保持 271 行。
- 📝[开发]-需求文档、更新日志和版本号同步本轮 Git status/history 输出解析职责拆分。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；需求文档禁用措辞扫描通过；`git diff --check` 通过；已生成 `docs/screenshots/git-status-history-output-parser.png` 做基础界面回归截图验证。

## 0.78.0.0 - 2026-05-28

- 🧩[开发]-新增 `GitRepositoryOutputParser`，集中维护 Git 分支、远端分支、ahead/behind 和最近提交展示文本的纯输出解析。
- 🧠[开发]-`GitRepositoryService` 移除重复解析 helper，只保留 Git 命令编排和仓库业务流程，服务行数降至 1658 行。
- 🧠[开发]-解析逻辑拆分为无状态静态组件，便于后续继续把 status、history、diff 等解析路径独立出去并补充窄测试。
- 📝[开发]-需求文档、更新日志和版本号同步本轮 Git 输出解析职责拆分。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；需求文档禁用措辞扫描通过；`git diff --check` 通过；已生成 `docs/screenshots/git-repository-output-parser.png` 做基础界面回归截图验证。

## 0.77.0.0 - 2026-05-28

- 🧩[开发]-新增 `IRepositoryOperationCommandService`、`RepositoryOperationCommandService` 和 `RepositoryOperationCommandRequest`，集中承载仓库操作状态命令的执行模板。
- 🧠[开发]-merge/rebase/revert/cherry-pick 的 continue、skip、abort 流程统一走服务化命令运行器，复用忙碌状态设置、状态消息、工作区刷新和失败回填逻辑。
- 🧠[开发]-`MainWindowViewModel` 移除三套重复操作状态 try/catch 模板，主 ViewModel 行数降至 3597 行。
- 📝[开发]-需求文档、更新日志和版本号同步本轮仓库操作状态命令服务化拆分。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；需求文档禁用措辞扫描通过；`git diff --check` 通过；已生成 `docs/screenshots/repository-operation-command-service.png` 做基础界面回归截图验证。

## 0.76.0.0 - 2026-05-28

- 🎨[开发]-主窗口补齐 `Ctrl+1` 和 `Ctrl+2` 真实快捷键处理，菜单显示的 Changes/History 快捷键现在有对应命令路径。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；需求文档禁用措辞扫描通过；`git diff --check` 通过；已生成 `docs/screenshots/changes-history-keybindings.png` 做基础界面回归截图验证。

## 0.75.0.0 - 2026-05-28

- 🧩[开发]-新增 `CommitFilesViewStateBuilder` 和 `CommitFilesViewState`，集中维护 History 选中提交文件项 ViewModel 构建和选中项恢复。
- 🧠[开发]-`MainWindowViewModel` 不再直接构建 History 文件项列表，主 ViewModel 行数降至 3633 行。
- 📝[开发]-需求文档、更新日志和版本号同步本轮 History 文件列表职责拆分。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；需求文档禁用措辞扫描通过；`git diff --check` 通过；已生成 `docs/screenshots/commit-files-view-state-builder.png` 做基础界面回归截图验证。

## 0.74.0.0 - 2026-05-28

- 🧩[开发]-新增 `BranchesViewStateBuilder` 和 `BranchesFilterResult`，集中维护分支列表过滤、当前分支优先选中和旧选中项恢复。
- 🧠[开发]-`MainWindowViewModel` 移除分支过滤匹配细节，主 ViewModel 行数降至 3636 行。
- 📝[开发]-需求文档、更新日志和版本号同步本轮分支列表职责拆分。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；需求文档禁用措辞扫描通过；`git diff --check` 通过；已生成 `docs/screenshots/branches-view-state-builder.png` 做基础界面回归截图验证。

## 0.73.0.0 - 2026-05-28

- 🧩[开发]-新增 `ChangedFilesViewStateBuilder`，集中维护 Changes 文件过滤、冲突过滤、选中项恢复和全选三态计算。
- 🧩[开发]-新增 `ChangedFilesFilterResult` 与 `ChangedFilesIncludedState` 结果模型，让 Changes 列表 UI 状态计算和主窗口属性通知分离。
- 🧠[开发]-`MainWindowViewModel` 移除 Changes 过滤匹配和全选三态计算细节，主 ViewModel 行数降至 3646 行。
- 📝[开发]-需求文档、更新日志和版本号同步本轮 Changes 列表职责拆分。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；需求文档禁用措辞扫描通过；`git diff --check` 通过；已生成 `docs/screenshots/changed-files-view-state-builder.png` 做基础界面回归截图验证。

## 0.72.0.0 - 2026-05-28

- 🧩[开发]-新增 `RepositoryListGroupBuilder`，集中维护仓库选择器过滤、Recent 分组、分组排序和当前仓库高亮路径比较。
- 🧠[开发]-`MainWindowViewModel` 只负责刷新 `RepositoryGroups` 集合，不再直接承载仓库列表构建细节，主 ViewModel 行数降至 3663 行。
- 📝[开发]-需求文档、更新日志和版本号同步本轮仓库列表职责拆分。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；需求文档禁用措辞扫描通过；`git diff --check` 通过；已生成 `docs/screenshots/repository-list-group-builder.png` 做基础界面回归截图验证。

## 0.71.0.0 - 2026-05-28

- 🧩[开发]-新增 `RepositorySyncStatus` 模型和 `RepositorySyncOperation` 模型枚举，远端同步状态从主 ViewModel 下沉到 Core 模型层。
- 🧩[开发]-新增 `IRepositorySyncStatusService` 和 `RepositorySyncStatusService`，集中维护同步按钮标题、说明、图标显隐和远端操作状态文案。
- 🧠[开发]-`MainWindowViewModel` 移除同步展示文案 switch、last fetched 格式化和同步徽标判断逻辑，主 ViewModel 行数降至 3717 行。
- 📝[开发]-需求文档、更新日志和版本号同步本轮同步状态职责拆分。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；需求文档禁用措辞扫描通过；`git diff --check` 通过；已生成 `docs/screenshots/repository-sync-status-service.png` 做基础界面回归截图验证。

## 0.70.0.0 - 2026-05-28

- 🧩[开发]-新增 `IRepositoryInteractionService` 和 `RepositoryInteractionService`，集中承载剪贴板、文件管理器、外部编辑器和 GitHub 浏览器入口。
- 🧠[开发]-`MainWindowViewModel` 不再直接依赖 `IRepositoryShellService`，仓库外部交互错误提示统一由交互服务生成并通过事件总线发布。
- 🧠[开发]-复制文本、打开仓库位置、显示变更文件、查看 GitHub 仓库/分支/提交/比较/PR/Issue 等重复 try/catch 逻辑从主 ViewModel 移出。
- 📝[开发]-需求文档、更新日志和版本号同步本轮职责拆分。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；需求文档禁用措辞扫描通过；`git diff --check` 通过；已生成 `docs/screenshots/repository-interaction-service.png` 做基础界面回归截图验证。

## 0.69.0.0 - 2026-05-28

- 🧩[开发]-`IGitRepositoryService` 和 `GitRepositoryService` 新增 `MarkConflictsResolvedAsync`，对冲突文件路径执行 `git add -- <paths>`。
- 🧩[开发]-`GitChangeItemViewModel` 新增 MarkResolved 命令，冲突文件项可直接触发标记已解决。
- 🧩[开发]-`MainWindowViewModel` 新增单个/批量冲突标记流程，复用仓库忙碌状态并在完成后刷新工作区。
- 🎨[开发]-Changes 文件项右键菜单新增 Mark conflict as resolved，列表空白右键菜单新增 Mark all conflicts as resolved。
- 🌐[开发]-补充冲突标记菜单和状态提示中英文资源，并同步 `AvaGithubDesktopL` 常量。
- 📝[开发]-需求文档、更新日志和版本号同步本轮冲突辅助能力。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；中英文 JSON 资源一致；需求文档禁用措辞扫描通过；`git diff --check` 通过；已生成 `docs/screenshots/keyboard-lfs-conflicts.png` 做基础界面截图验证。

## 0.68.0.0 - 2026-05-28

- 🧩[开发]-`IGitRepositoryService` 和 `GitRepositoryService` 新增 `FetchLfsAsync`、`PullLfsAsync`，分别执行 `git lfs fetch` 与 `git lfs pull`。
- 🧩[开发]-`MainWindowViewModel` 新增 LFS 同步命令，并接入现有 `RepositorySyncOperation` 状态机。
- 🎨[开发]-Repository 菜单新增 Fetch LFS objects 和 Pull LFS files 入口，复用同步启用状态。
- 🌐[开发]-补充 Git LFS 菜单、同步标题和状态提示中英文资源，并同步 `AvaGithubDesktopL` 常量。
- 📝[开发]-需求文档、更新日志和版本号同步本轮 Git LFS 基础入口。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；中英文 JSON 资源一致；需求文档禁用措辞扫描通过；`git diff --check` 通过。

## 0.67.0.0 - 2026-05-28

- 🎨[开发]-主窗口为仓库选择器、分支选择器和 Changes 过滤框补充命名引用。
- 🧩[开发]-`MainWindow.axaml.cs` 新增 `Ctrl+T`、`Ctrl+B`、`Ctrl+L` 键盘焦点控制，打开对应弹出层后自动聚焦过滤输入框。
- 🎨[开发]-View 菜单新增 Repository list、Branches list 和 Changes filter 入口，并显示对应快捷键。
- 🌐[开发]-补充 View 菜单快捷入口中英文资源，并同步 `AvaGithubDesktopL` 常量。
- 📝[开发]-快捷键说明文档、需求文档、更新日志和版本号同步本轮键盘导航优化。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；中英文 JSON 资源一致；需求文档禁用措辞扫描通过；`git diff --check` 通过。

## 0.66.0.0 - 2026-05-28

- 🧩[开发]-`MainWindowViewModel` 新增快捷键说明窗口命令，HelpService 支持加载 `docs/快捷键.md`。
- 🎨[开发]-主菜单补充 GitHub Desktop 常用快捷键标记，并在 View 菜单新增 Changes/History 跳转入口。
- 🎨[开发]-主窗口新增 `Ctrl+Enter` 提交快捷键，复用现有提交命令和启用状态。
- 🌐[开发]-补充快捷键菜单、窗口标题和状态提示中英文资源，并同步 `AvaGithubDesktopL` 常量。
- 📝[开发]-新增快捷键说明文档并随应用输出；更新需求文档、更新日志和版本号。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；中英文 JSON 资源一致；需求文档禁用措辞扫描通过；`git diff --check` 通过。

## 0.65.0.0 - 2026-05-27

- 🧩[开发]-`MainWindowViewModel` 新增默认分支名复制命令和启用状态。
- 🎨[开发]-Branch 菜单新增 Copy default branch name 入口，放在 Update from default branch 附近。
- 🌐[开发]-补充默认分支复制菜单和状态提示中英文资源，并同步 `AvaGithubDesktopL` 常量。
- 📝[开发]-需求文档已完成功能和使用流程同步默认分支复制入口。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；中英文 JSON 资源一致；需求文档禁用措辞扫描通过。

## 0.64.0.0 - 2026-05-27

- 🧩[开发]-`MainWindowViewModel` 新增上游分支名复制命令和启用状态。
- 🎨[开发]-Branch 菜单新增 Copy upstream branch name 入口。
- 🌐[开发]-补充上游分支复制菜单和状态提示中英文资源，并同步 `AvaGithubDesktopL` 常量。
- 📝[开发]-需求文档已完成功能和使用流程同步上游分支复制入口。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；中英文 JSON 资源一致；需求文档禁用措辞扫描通过。

## 0.63.0.0 - 2026-05-27

- 🧩[开发]-`MainWindowViewModel` 新增远端 URL 复制命令，按当前仓库和远端地址状态启用。
- 🎨[开发]-Repository 菜单新增 Copy remote URL 入口。
- 🌐[开发]-补充复制远端 URL 菜单和状态提示中英文资源，并同步 `AvaGithubDesktopL` 常量。
- 📝[开发]-需求文档已完成功能和使用流程同步远端 URL 复制入口。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；中英文 JSON 资源一致；需求文档禁用措辞扫描通过。

## 0.62.0.0 - 2026-05-27

- 🧩[开发]-`MainWindowViewModel` 新增当前仓库名称和路径复制命令。
- 🎨[开发]-Repository 菜单新增当前仓库复制入口，复用已有仓库复制文案和状态提示。
- 📝[开发]-需求文档已完成功能和使用流程同步当前仓库复制入口。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；中英文 JSON 资源一致；需求文档禁用措辞扫描通过。

## 0.61.0.0 - 2026-05-27

- 🧩[开发]-`MainWindowViewModel` 新增当前分支复制命令和启用状态。
- 🎨[开发]-Branch 菜单新增 Copy branch name 入口，复用已有分支复制文案和状态提示。
- 📝[开发]-需求文档已完成功能和使用流程同步当前分支复制入口。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；中英文 JSON 资源一致；需求文档禁用措辞扫描通过。

## 0.60.0.0 - 2026-05-27

- 🧩[开发]-`RepositoryHistoryService` 新增移除已知仓库记录能力，并复用统一的历史记录写入逻辑。
- 🎨[开发]-仓库列表右键菜单新增 Remove from list 命令，执行后刷新仓库分组列表。
- 🌐[开发]-补充移除仓库列表项菜单和状态提示中英文资源，并同步 `AvaGithubDesktopL` 常量。
- 📝[开发]-需求文档已完成功能和使用流程同步仓库列表移除入口。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；中英文 JSON 资源一致；需求文档禁用措辞扫描通过。

## 0.59.0.0 - 2026-05-27

- 🧩[开发]-`GitRepositoryService` 新增 `UpdateSubmodulesAsync`，执行 `git submodule update --init --recursive`。
- 🧩[开发]-Repository 菜单新增 Update submodules 命令，复用同步忙碌状态和状态栏提示。
- 🌐[开发]-补充子模块更新菜单、同步标题和状态提示中英文资源，并同步 `AvaGithubDesktopL` 常量。
- 📝[开发]-需求文档已完成功能和使用流程同步子模块更新入口。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；中英文 JSON 资源一致；临时仓库子模块 update --init --recursive 验证通过。

## 0.58.0.0 - 2026-05-27

- 🧩[开发]-`GitRepositoryService` 新增 `FetchAllAsync`，执行 `git fetch --all --prune --recurse-submodules=on-demand`。
- 🧩[开发]-Repository 菜单新增 Fetch all remotes 命令，并纳入现有同步状态机和状态栏提示。
- 🌐[开发]-补充获取全部远端菜单、同步标题和状态提示中英文资源，并同步 `AvaGithubDesktopL` 常量。
- 📝[开发]-需求文档已完成功能和使用流程同步 Fetch all remotes 入口。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；中英文 JSON 资源一致；临时仓库多远端 fetch --all 验证通过。

## 0.57.0.0 - 2026-05-27

- 🎨[开发]-Changes 头部新增 Conflicts 过滤开关，仅在存在冲突文件时显示。
- 🧩[开发]-`MainWindowViewModel` 新增 `ShowConflictsOnly`、冲突文件计数和组合过滤逻辑，保留隐藏文件原勾选状态。
- 🌐[开发]-补充冲突过滤开关中英文资源，并同步 `AvaGithubDesktopL` 常量。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；中英文 JSON 资源一致；需求文档禁用措辞扫描通过。

## 0.56.1.0 - 2026-05-27

- 🎨[开发]-`GitChangeItem` 统一识别 DD、AU、UD、UA、DU、AA、UU 等 unmerged 状态，显示为 Conflict。
- 🎨[开发]-冲突文件状态徽标改用红色提示，并向 `GitChangeItemViewModel` 暴露 `IsConflict`。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；临时仓库冲突状态码验证通过；需求文档禁用措辞扫描通过。

## 0.56.0.0 - 2026-05-27

- 🧩[开发]-`GitRepositoryService` 新增 `SetDefaultBranchAsync`，通过 `git remote set-head` 更新当前远端的本地默认分支引用。
- 🧩[开发]-Branch 菜单新增 Set current branch as default 命令，启用状态跟随当前分支、远端和默认分支。
- 🌐[开发]-补充默认分支设置菜单和状态提示中英文资源，并同步 `AvaGithubDesktopL` 常量。
- 📝[开发]-需求文档已完成功能、使用流程和计划清单同步默认分支管理入口。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；中英文 JSON 资源一致；临时仓库 `remote set-head` 验证通过。

## 0.55.1.0 - 2026-05-27

- 🎨[开发]-Changes 区域新增仓库操作冲突提示横幅，按 merge、rebase、revert、cherry-pick 状态显示对应处理路径。
- 🌐[开发]-补充冲突提示横幅中英文资源，并同步 `AvaGithubDesktopL` 常量。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；中英文 JSON 资源一致；需求文档禁用措辞扫描通过。

## 0.55.0.0 - 2026-05-27

- 🧩[开发]-`GitRepositoryService` 新增 cherry-pick、continue 和 abort 能力，并识别 `CHERRY_PICK_HEAD` 操作状态。
- 🎨[开发]-History 选中提交新增 Cherry-pick 按钮，Branch 菜单新增 Cherry-pick 冲突恢复入口。
- 🌐[开发]-补充 Cherry-pick 按钮、菜单和状态提示中英文资源，并同步 `AvaGithubDesktopL` 常量。
- 📝[开发]-需求文档已完成功能和使用流程同步 History Cherry-pick 入口。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；中英文 JSON 资源一致；临时仓库覆盖非冲突 cherry-pick、冲突检测和 abort。

## 0.54.0.0 - 2026-05-27

- 🧩[开发]-`GitRepositoryService` 新增远端分支读取和 `SetUpstreamAsync`，通过 `git branch --set-upstream-to` 设置当前分支上游。
- 🧩[开发]-Branch 菜单新增 Set upstream 对话框，列出远端分支并优先选中同名远端分支。
- 🌐[开发]-补充设置上游分支菜单、对话框和状态提示中英文资源，并同步 `AvaGithubDesktopL` 常量。
- 📝[开发]-需求文档已完成功能、使用流程和计划清单同步设置上游入口。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；中英文 JSON 资源一致；临时仓库设置上游验证通过。

## 0.53.1.0 - 2026-05-27

- 🎨[开发]-再次简化 `app-logo.svg`，去掉分支弧线和多节点，只保留 A 字母主体与单个绿色状态点。
- 🎨[开发]-重新生成 `app-logo.png` 与多尺寸 `app-logo.ico`，继续沿用现有资源引用。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；确认 PNG 预览清晰，ICO/PNG/SVG 资源已重新生成。

## 0.53.0.0 - 2026-05-27

- 🧩[开发]-`GitRepositoryService` 新增 `UnsetUpstreamAsync`，通过 `git branch --unset-upstream` 清理当前分支上游。
- 🧩[开发]-Branch 菜单新增 Unset upstream 命令，启用状态跟随当前分支 upstream。
- 🌐[开发]-补充取消上游分支菜单与状态提示中英文资源，并同步 `AvaGithubDesktopL` 常量。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；中英文 JSON 资源一致；临时仓库上游取消验证通过。

## 0.52.0.0 - 2026-05-27

- 🧩[开发]-仓库操作状态枚举新增 Revert，快照加载时识别 `.git/REVERT_HEAD`。
- 🧩[开发]-`GitRepositoryService` 新增 revert continue/abort 能力，Branch 菜单按状态显示对应恢复入口。
- 🌐[开发]-补充 Continue/Abort revert 菜单和状态提示中英文资源，并同步 `AvaGithubDesktopL` 常量。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；中英文 JSON 资源一致；临时仓库覆盖 revert continue 和 abort。

## 0.51.0.0 - 2026-05-27

- 🧩[开发]-`GitRepositoryService` 新增 `PushTagsAsync`，执行 `git push <remote> --tags`。
- 🧩[开发]-Repository 菜单新增 Push tags 命令，复用 `IsSyncing` 状态和仓库刷新流程。
- 🌐[开发]-补充 Push tags 菜单与状态提示中英文资源，并同步 `AvaGithubDesktopL` 常量。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；中英文 JSON 资源一致；临时 bare remote 验证标签推送通过。

## 0.50.0.0 - 2026-05-27

- 🧩[开发]-`GitRepositoryService` 新增 `RevertCommitAsync`，执行 `git revert --no-edit` 创建还原提交。
- 🧩[开发]-History 提交详情新增 Revert 按钮，ViewModel 在成功或冲突后统一刷新仓库快照、分支和历史。
- 🌐[开发]-补充 Revert 按钮和状态提示中英文资源，并同步 `AvaGithubDesktopL` 常量。
- 📝[开发]-需求文档已完成功能和使用流程同步 History Revert 入口。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；中英文 JSON 资源一致；临时仓库非冲突 revert 验证通过。

## 0.49.0.0 - 2026-05-27

- 🧩[开发]-新增标签创建请求模型、标签对话框服务、ViewModel 和窗口，History 选中提交可打开 Create tag 流程。
- 🧩[开发]-`GitRepositoryService` 新增标签列表读取与标签创建能力，支持 lightweight tag 和 annotated tag。
- 🌐[开发]-补充创建标签对话框、按钮、校验和状态提示中英文资源，并同步 `AvaGithubDesktopL` 常量。
- 📝[开发]-需求文档已完成功能和使用流程同步 History 创建标签入口。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；中英文 JSON 资源一致；临时仓库 lightweight/annotated tag 创建和重复标签失败验证通过。

## 0.48.0.0 - 2026-05-27

- 🧩[开发]-仓库快照新增 HEAD 提交摘要，提交面板开启 Amend 时可自动预填提交标题。
- 🧩[开发]-`GitRepositoryService.CommitAsync` 增加 amend 参数，保留选择性提交路径处理，并允许无文件变更时仅修正提交信息。
- 🎨[开发]-Changes 提交区域新增 Amend last commit 复选框，提交按钮文案随 amend 状态切换。
- 🌐[开发]-补充 amend 提交相关中英文资源，并同步 `AvaGithubDesktopL` 常量。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；中英文 JSON 资源一致；临时仓库覆盖带文件 amend 与仅改提交信息 amend。

## 0.47.0.0 - 2026-05-27

- 🧩[开发]-新增仓库操作状态枚举，快照加载时识别 merge 与 rebase 进行中状态。
- 🧩[开发]-`GitRepositoryService` 新增 merge continue/abort 与 rebase continue/skip/abort 命令，Git 进程默认关闭编辑器等待。
- 🧩[开发]-Branch 菜单在冲突状态下显示对应继续、跳过、终止入口，并禁用新的分支切换、合并、变基和同步操作。
- 🌐[开发]-补充冲突处理菜单与状态提示中英文资源，并同步 `AvaGithubDesktopL` 常量。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；中英文 JSON 资源一致；临时仓库覆盖 merge continue/abort 与 rebase continue/skip/abort。

## 0.46.0.0 - 2026-05-27

- 🧩[开发]-新增 `RepositoryRemoteRequest`、远端管理对话框服务、ViewModel 和窗口，Repository 菜单可打开 Manage remote 流程。
- 🧩[开发]-`GitRepositoryService` 新增远端 set-url/add/remove 能力，保存或移除后统一刷新仓库快照、分支和历史。
- 🌐[开发]-补充远端管理对话框、菜单和状态提示中英文资源，并同步 `AvaGithubDesktopL` 常量。
- 📝[开发]-需求文档的软件使用流程、已完成和计划功能同步远端管理入口。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；中英文 JSON 资源一致；临时仓库远端 add、set-url、remove 验证通过。

## 0.45.1.0 - 2026-05-27

- 🎨[开发]-重新绘制 `app-logo.svg`，移除复杂窗口外形，改为深色圆角底、白色 A 字母和轻量分支节点。
- 🎨[开发]-使用同一视觉重新生成 `app-logo.png` 与多尺寸 `app-logo.ico`，保持项目引用路径不变。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；确认 PNG 预览清晰，ICO/PNG/SVG 资源已重新生成。

## 0.45.0.0 - 2026-05-27

- 🧩[开发]-新增添加现有仓库请求模型、对话框服务、ViewModel 和窗口，替换原先直接打开目录选择器的流程。
- 🌐[开发]-补充添加现有仓库对话框标题、路径标签和路径校验中英文资源，并同步 `AvaGithubDesktopL` 常量。
- 📝[开发]-需求文档已完成功能补充添加已有仓库完整引导，计划清单移除对应待办。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；中英文 JSON 资源一致；新增对话框、服务注册和命令引用扫描正常。

## 0.44.0.0 - 2026-05-27

- 🧩[开发]-新增创建仓库请求模型、对话框服务、ViewModel 和窗口，创建流程继续使用 MVVM 命令关闭。
- 🧩[开发]-`GitRepositoryService` 新增 `CreateRepositoryAsync`，创建目标目录后执行 `git init`，失败时清理新建目录。
- 🌐[开发]-补充 New repository 菜单、对话框、校验和状态提示中英文资源，并同步 `AvaGithubDesktopL` 常量。
- 📝[开发]-需求文档的软件使用流程、已完成和计划功能同步创建仓库入口。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；中英文 JSON 资源一致；临时目录 `git init` 验证通过。

## 0.43.0.0 - 2026-05-27

- 🧩[开发]-新增 Clone 仓库请求模型、对话框服务、ViewModel 和窗口，克隆流程保持 MVVM 命令关闭。
- 🧩[开发]-`GitRepositoryService` 新增 `CloneRepositoryAsync`，执行 `git clone` 后由主窗口自动打开目标仓库。
- 🌐[开发]-补充 Clone 仓库菜单、对话框、校验和状态提示中英文资源，并同步 `AvaGithubDesktopL` 常量。
- 📝[开发]-需求文档的软件使用流程和已完成功能补充 Clone 仓库入口。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；中英文 JSON 资源一致；临时 bare 仓库 clone 命令验证通过。

## 0.42.4.0 - 2026-05-27

- 📝[开发]-重写需求文档为简单功能清单，分为项目定位、开发约定、已完成、使用流程、进行中和计划功能。
- 📝[开发]-新增 Mermaid 软件使用流程图，后续功能迭代时继续同步完善主路径。
- 🔧[开发]-移除首次启动固定开发仓库回退；没有上次仓库时保持空状态，等待用户选择仓库。
- 🔧[开发]-仓库历史服务不再扫描开发环境目录，仓库选择器只使用用户打开过的有效仓库记录。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；需求文档内容检查和本地路径扫描通过。

## 0.42.3.0 - 2026-05-27

- 🧩[开发]-新增 `Themes/Shared/Octicons.axaml`，集中维护 GitHub Desktop 同源 Octicon `StreamGeometry` 资源。
- 🎨[开发]-`MainWindow.axaml` 的仓库、分支、同步和下拉图标改为引用 `StaticResource`，减少长路径重复和界面文件噪音。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；中英文 JSON 资源一致；MVVM 事件扫描和内联长路径扫描无命中；截图确认图标资源运行时解析正常。

## 0.42.2.0 - 2026-05-27

- 🎨[开发]-参考 GitHub Desktop `PushPullButton`，将顶部同步入口的字符箭头替换为同源 Octicon 风格矢量图标。
- 🧩[开发]-`MainWindowViewModel` 新增同步图标和 ahead/behind 徽标派生属性，XAML 只做状态绑定，保持 MVVM 展示边界。
- 🎨[开发]-新增同步按钮右侧独立下拉入口，Fetch、Pull、Push 继续绑定现有 ViewModel 命令。
- 🎨[开发]-仓库、分支、同步下拉箭头统一使用 triangle-down 矢量路径，去掉工具栏残留字符图标。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；中英文 JSON 资源一致；MVVM 事件扫描无命中；`git diff --check` 仅有 CRLF 提示。
- 🧪[验证]-启动程序截图更新 `docs/screenshots/sync-toolbar.png`，确认同步图标、下拉箭头和工具栏布局正常；UIAutomation 确认同步下拉包含“获取、拉取、推送”。

## 0.42.1.0 - 2026-05-27

- 🎨[开发]-参考 GitHub Desktop Octicon 资源，将顶部 Current Repository 和 Current Branch 的字符图标替换为 repo 与 git-branch 矢量图标。
- 🧩[开发]-新增 `Button.toolbarSplit PathIcon.toolbarOcticon` 样式，统一顶栏矢量图标尺寸、颜色和悬停状态。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；确认顶栏仓库、分支图标使用主题样式而非内联颜色。

## 0.42.0.0 - 2026-05-27

- 🎨[开发]-参考 GitHub Desktop 标题栏和工具栏分工，移除工具栏右侧账号块与语言下拉，保留仓库、分支、同步三个核心操作区。
- 🧩[开发]-File 标题栏菜单补充当前账号信息展示，登录、退出登录和 View 菜单语言切换继续使用既有 MVVM 命令与持久化逻辑。
- 🎨[开发]-调整默认浅色资源色板为深色冷调工具栏、青色强调线、浅冷色选中态和悬停态，提升默认界面科技感。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；UIAutomation 确认顶部工具栏不再显示账号与语言直出控件；截图检查默认配色与三段式工具栏显示正常。

## 0.41.1.0 - 2026-05-27

- 🎨[开发]-参考 `docs/GithubDesktop/3.png`、`repository-list-item.tsx`、`repositories-list.tsx` 和 `_repository-list.scss`，重做仓库过滤列表行结构。
- 🧩[开发]-仓库行改用 GitHub Desktop Octicon repo 路径图标、固定 29px 行高、右侧 7px 当前仓库圆点，并将样式封装到 Shell 列表主题文件。
- 🧩[开发]-`MainWindowViewModel` 的 Recent 分组排除当前仓库，避免同一仓库在 Recent 和所属分组里重复高亮。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；UIAutomation 打开仓库选择器确认行高 29px、仓库名称左对齐、Recent 不重复当前仓库。

## 0.41.0.0 - 2026-05-27

- 🌿[开发]-参考 GitHub Desktop Branch 主菜单顺序，新增当前分支 Rename 和 Delete 菜单入口。
- 🧩[开发]-`MainWindowViewModel` 新增 `RenameCurrentBranchCommand` 与 `DeleteCurrentBranchCommand`，复用已有分支右键菜单的重命名、删除流程。
- 🧩[开发]-新增 `CanRenameCurrentBranch` 与 `CanDeleteCurrentBranch`，分支状态变化时统一刷新菜单启用状态，当前分支删除保持禁用。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；中英文 JSON 资源一致；MVVM 点击事件扫描无命中；`git diff --check` 仅有 CRLF 提示。
- 🧪[验证]-UIAutomation 启动后自动恢复 AvaGithubDesktop 仓库，确认 6 个变更文件全部选中，并生成 `docs/screenshots/branch-current-actions.png` 截图。

## 0.40.1.0 - 2026-05-27

- 🔧[开发]-调整启动仓库选择逻辑，优先读取 `App.config` 中的最后打开仓库路径。
- 🧩[开发]-`AppSettings` 和 `AppSettingsStore` 新增 `LastRepositoryPath` 读写，仓库成功打开后立即持久化。
- 📝[开发]-`ResolveDefaultRepositoryPath` 保留开发默认仓库回退，并增加中文注释说明首次启动与后续启动的差异。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；中英文 JSON 资源一致；MVVM 点击事件扫描无命中；`git diff --check` 仅有 CRLF 提示。
- 🧪[验证]-新版程序打开 AvaGithubDesktop 后写入输出目录 `App.config` 的 `LastRepositoryPath`，重启后自动恢复 AvaGithubDesktop 仓库，并生成 `docs/screenshots/last-repository-restore.png` 截图。

## 0.40.0.0 - 2026-05-27

- 🌿[开发]-参考 GitHub Desktop `create-issue-in-repository-on-github` 菜单，Repository 主菜单新增创建 Issue 入口。
- 🧩[开发]-`RepositoryRemoteUrlHelper` 新增 `/issues/new/choose` URL 生成逻辑，保持和 GitHub Desktop 打开 issue 模板选择页一致。
- 🧩[开发]-`MainWindowViewModel` 新增 `CreateIssueOnGitHubCommand`，复用仓库 GitHub 远端识别逻辑控制菜单启用状态。
- 🌐[开发]-补充创建 Issue 菜单和状态提示中英文资源，并同步更新 T4 生成的 `AvaGithubDesktopL` 常量文件。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；中英文 JSON 资源一致；MVVM 点击事件扫描无命中；`git diff --check` 仅有 CRLF 提示。
- 🧪[验证]-临时 .NET 控制台验证 GitHub Issue URL 生成为 `/issues/new/choose`；UIAutomation 切换到 AvaGithubDesktop 仓库并生成 `docs/screenshots/create-issue-menu.png` 界面截图。

## 0.39.0.0 - 2026-05-27

- 🌿[开发]-参考 GitHub Desktop `Create Pull Request` 菜单和 `_openCreatePullRequestInBrowser`，Branch 主菜单新增创建 Pull Request 入口。
- 🧩[开发]-`RepositoryRemoteUrlHelper` 新增 `/pull/new/{branch}` URL 生成逻辑，优先使用 upstream 分支名，未发布分支回退当前本地分支名。
- 🧩[开发]-`MainWindowViewModel` 新增 `CreatePullRequestCommand`，菜单命令继续通过 ReactiveCommand 的 CanExecute 管理启用状态。
- 🌐[开发]-补充创建 Pull Request 菜单和状态提示中英文资源，并同步更新 T4 生成的 `AvaGithubDesktopL` 常量文件。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；中英文 JSON 资源一致；MVVM 点击事件扫描无命中；`git diff --check` 仅有 CRLF 提示。
- 🧪[验证]-临时 .NET 控制台验证 GitHub Pull Request URL 对已发布分支和未发布分支均生成 `/pull/new/{branch}`；UIAutomation 切换到 AvaGithubDesktop 仓库并生成 `docs/screenshots/create-pull-request-menu.png` 界面截图。

## 0.38.0.0 - 2026-05-27

- 🌿[开发]-参考 GitHub Desktop `Update from <default branch>` 行为，Branch 主菜单新增从默认分支更新入口。
- 🧩[开发]-`GitRepositoryService` 加载仓库快照时解析默认分支，优先读取 `origin/HEAD`，再回退本地 `main/master`。
- 🧩[开发]-`MainWindowViewModel` 新增 `UpdateFromDefaultBranchCommand`，复用普通 merge 流程更新当前分支并处理 already up-to-date。
- 🌐[开发]-补充默认分支更新菜单和状态提示中英文资源，并同步更新 T4 生成的 `AvaGithubDesktopL` 常量文件。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；中英文 JSON 资源一致；MVVM 点击事件扫描无命中；临时 Git 仓库验证默认分支解析为 `main`，首次更新返回 `Success`，重复更新返回 `AlreadyUpToDate`。
- 🧪[验证]-UIAutomation 加载 AvaGithubDesktop 仓库并截图确认 Changes 列表、Diff 预览和操作日志显示正常。

## 0.37.0.0 - 2026-05-27

- 🌿[开发]-参考 GitHub Desktop `Squash and Merge into Current Branch...` 流程，Branch 主菜单新增压缩合并入口。
- 🧩[开发]-`MergeBranchWindowViewModel` 增加 squash merge 模式，复用现有分支选择弹窗并切换标题、说明和按钮文案。
- 🧩[开发]-`GitRepositoryService` 新增 `SquashMergeBranchAsync`，执行 `git merge --squash <branch>` 后再 `git commit --no-edit`。
- 🌐[开发]-补充压缩合并菜单、弹窗和状态提示中英文资源，并同步更新 T4 生成的 `AvaGithubDesktopL` 常量文件。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；中英文 JSON 资源一致；MVVM 点击事件扫描无命中；临时 Git 仓库验证 squash merge 返回 `Success` 并创建压缩提交，对祖先分支返回 `AlreadyUpToDate`。
- 🧪[验证]-UIAutomation 加载 AvaGithubDesktop 仓库并截图确认 Changes 列表、Diff 预览和操作日志显示正常。

## 0.36.0.0 - 2026-05-27

- 🌿[开发]-参考 GitHub Desktop `Rebase Current Branch...` 流程，Branch 主菜单新增当前分支变基入口。
- 🧩[开发]-新增 `RebaseBranchWindowViewModel`、`RebaseBranchWindow`、`BranchRebaseRequest` 和 `GitRebaseResult`，变基弹窗继续使用 MVVM 命令关闭。
- 🧩[开发]-`GitRepositoryService` 新增 `RebaseCurrentBranchAsync`，执行 `git rebase <baseBranch>` 并识别 already up-to-date 结果。
- 🌐[开发]-补充分支变基菜单、弹窗和状态提示中英文资源，并同步更新 T4 生成的 `AvaGithubDesktopL` 常量文件。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；中英文 JSON 资源一致；MVVM 点击事件扫描无命中；临时 Git 仓库验证首次 rebase 返回 `Success`，重复 rebase 返回 `AlreadyUpToDate`。

## 0.35.0.0 - 2026-05-27

- 🌿[开发]-参考 GitHub Desktop `Merge into Current Branch...` 流程，Branch 主菜单新增合并分支入口。
- 🧩[开发]-新增 `MergeBranchWindowViewModel`、`MergeBranchWindow`、`BranchMergeRequest` 和 `GitMergeResult`，合并弹窗继续使用 MVVM 命令关闭。
- 🧩[开发]-`GitRepositoryService` 新增 `MergeBranchAsync`，执行 `git merge <branch>` 并识别 already up-to-date 结果。
- 🌐[开发]-补充分支合并菜单、弹窗和状态提示中英文资源，并同步更新 T4 生成的 `AvaGithubDesktopL` 常量文件。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；中英文 JSON 资源一致；MVVM 点击事件扫描无命中；临时 Git 仓库验证首次合并返回 `Success`，重复合并返回 `AlreadyUpToDate`。

## 0.34.0.0 - 2026-05-27

- 🌿[开发]-参考 GitHub Desktop `openBranchOnGitHub('compare')`，Branch 主菜单新增 Compare on GitHub。
- 🧩[开发]-`RepositoryRemoteUrlHelper` 抽出分支路由 URL 生成逻辑，`tree` 与 `compare` 共用 upstream 分支解析。
- 🌐[开发]-补充 Compare on GitHub 菜单与状态提示中英文资源，并同步更新 T4 生成的 `AvaGithubDesktopL` 常量文件。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；中英文 JSON 资源一致；MVVM 点击事件扫描无命中；`git diff --check` 仅有 CRLF 提示。

## 0.33.0.0 - 2026-05-27

- 🌿[开发]-参考 GitHub Desktop Branch 菜单，为当前分支补充“在 GitHub 上查看分支”入口。
- 🧩[开发]-新增 `ViewCurrentBranchOnGitHubCommand`，复用分支右键菜单已实现的 upstream 分支 URL 生成和打开逻辑。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；中英文 JSON 资源一致；MVVM 点击事件扫描无命中；Branch 菜单入口仅绑定 ViewModel 命令。

## 0.32.0.0 - 2026-05-27

- 🌿[开发]-参考 GitHub Desktop `push-pull-button` 的 `Publish branch` 判断，当前分支没有 upstream 时同步按钮改为发布分支。
- 🧩[开发]-新增 `RepositorySyncOperation.Publish`，继续复用 `GitRepositoryService.PushAsync` 的 `push --set-upstream` 行为完成首次发布。
- 🌐[开发]-补充发布分支按钮、描述和操作状态中英文资源，并同步更新 T4 生成的 `AvaGithubDesktopL` 常量文件。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；中英文 JSON 资源一致；MVVM 点击事件扫描无命中；临时 Git 仓库验证无 upstream 分支发布后建立 `origin/feature/publish-validation`。

## 0.31.0.0 - 2026-05-27

- 🌿[开发]-参考 GitHub Desktop `branch-dropdown` 的 `View Branch on GitHub` 行为，分支右键菜单新增打开 GitHub 分支页面命令。
- 🧩[开发]-`RepositoryRemoteUrlHelper` 新增 GitHub 分支 URL 生成逻辑，仅在分支存在 upstream 时使用去掉远端名前缀后的分支名拼接 `/tree/{branch}`。
- 🌐[开发]-补充分支 GitHub 菜单与状态提示中英文资源，并同步更新 T4 生成的 `AvaGithubDesktopL` 常量文件。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；中英文 JSON 资源一致；MVVM 点击事件扫描无命中；UIAutomation 确认 Current Branch 弹出层可读取 `main` 与 `origin/main`。

## 0.30.2.0 - 2026-05-27

- 🐛[修复]-`GitRepositoryService.RunGitAsync` 显式使用 UTF-8 读取 Git stdout/stderr，避免 Git diff 中的中文注释被系统 ANSI 解码成乱码。
- 🐛[修复]-`RunGitToFileAsync` 和 `RepositoryHistoryService` 的 Git stderr/stdout 解码同步使用 UTF-8，保持远程地址、错误信息和二进制导出流程一致。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；中英文 JSON 资源一致；MVVM 点击事件扫描无命中；截图确认中文 Diff 与左侧文件列表显示正常。

## 0.30.1.0 - 2026-05-27

- 🎨[优化]-`Lists.axaml` 为 Changes、History、Branch 和仓库选择器列表项补充显式 `Foreground`，避免选中/悬停状态继承主题默认前景色导致文字发浅。
- 🎨[优化]-仓库列表项、文件路径、提交摘要和分支名称统一使用 `PrimaryTextBrush`，辅助信息继续使用 `MutedTextBrush`。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；截图确认左侧 Changes 文件列表在选中状态下文件名与状态徽标可清晰阅读。

## 0.30.0.0 - 2026-05-27

- 🌿[开发]-参考 GitHub Desktop `branch-list-item-context-menu`、`rename-branch-dialog` 和 `delete-branch-dialog`，为 Current Branch 列表项补齐分支右键菜单。
- 🧩[开发]-新增 `GitBranchItemViewModel` 包装分支模型，分支项命令自包含，主窗口只负责注入重命名、复制和删除回调。
- 🧩[开发]-`GitRepositoryService` 新增 `RenameBranchAsync` 与 `DeleteBranchAsync`，分支重命名继续先使用 `git check-ref-format --branch` 校验目标名称。
- 🧩[开发]-新增重命名分支与删除分支对话框，弹窗按钮继续走 ViewModel 命令关闭，代码隐藏只保留关闭桥接。
- 🌐[开发]-补充分支重命名、删除、复制相关中英文资源，并同步更新 T4 生成的 `AvaGithubDesktopL` 常量文件。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；中英文 JSON 资源一致；MVVM 点击事件扫描无命中。
- 🧪[验证]-临时 Git 仓库完成分支重命名与删除流程，确认本地分支管理命令可用。

## 0.29.0.0 - 2026-05-27

- 🌿[开发]-参考 GitHub Desktop `branches-container`、`branch-list` 和 `create-branch-dialog`，在 Branch 菜单和 Current Branch 弹出层新增创建分支入口。
- 🧩[开发]-新增 `BranchCreationRequest`、`IBranchDialogService`、`BranchDialogService`、`CreateBranchWindowViewModel` 和 `CreateBranchWindow`，创建分支弹窗继续使用 MVVM 命令关闭。
- 🧩[开发]-`GitRepositoryService` 新增 `CreateBranchAsync`，先使用 `git check-ref-format --branch` 做权威校验，再创建分支并按 Desktop 交互自动 checkout。
- 🎨[开发]-新增分支创建弹窗、分支基准提示和 New branch 按钮样式，样式继续维护在 `AvaGithubDesktop.Controls.Themes`。
- 🌐[开发]-补充分支创建相关中英文资源，并同步更新 T4 生成的 `AvaGithubDesktopL` 常量文件。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；中英文 JSON 资源一致；MVVM 点击事件扫描无命中。
- 🧪[验证]-临时 Git 仓库创建并切换 `feature/create-branch-validation` 通过；UIAutomation 打开 Current Branch 弹出层和创建分支对话框，输入分支名后创建按钮正确启用。

## 0.28.0.0 - 2026-05-27

- 🧩[开发]-`GitRepositoryService` 对未跟踪文件增加 `ls-files --error-unmatch` 判断，未跟踪文本文件会生成最小 unified diff。
- 🧩[开发]-未跟踪文件读取前增加二进制探测，包含空字节的文件直接走 `BinaryDiffViewer`。
- 📝[开发]-新增文件 diff 生成逻辑补充中文注释，说明 Git 不会直接为未跟踪文件输出工作区 diff。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；临时未跟踪文本文件在 Changes 过滤后显示为 Untracked，并进入右侧统一 Diff 预览列表。

## 0.27.0.0 - 2026-05-27

- 🖼️[开发]-参考 GitHub Desktop `image-diffs` 与 `_diff.scss`，新增 `ImageDiffViewer`，支持 2-up、Swipe、Onion Skin、Difference 四种图片预览模式。
- 🧩[开发]-新增 `GitFileDiffPreview` 和 `GitDiffPreviewKind`，将右侧差异预览从单一字符串提升为文本、图片、二进制三类模型。
- 🧩[开发]-`GitRepositoryService` 新增图片 blob 导出缓存和 `diff --numstat` 二进制识别，图片缓存写入本地应用数据目录，不污染仓库。
- 🎨[开发]-新增 `BinaryDiffViewer` 与图片/二进制差异主题文件，样式继续维护在 `AvaGithubDesktop.Controls.Themes`。
- 🌐[开发]-补充图片差异模式、尺寸信息、二进制提示的中英文资源，并同步 `AvaGithubDesktopL` 常量。
- 🐛[修复]-`RepositoryHistoryService` 读取最近仓库记录时会过滤不完整条目，避免损坏的用户数据阻断仓库打开流程。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；JSON 资源可解析；MVVM 点击事件扫描无命中；`git diff --check` 仅有既有 CRLF 提示。
- 🧪[验证]-临时图片变更仓库加载通过，UIAutomation 确认右侧显示 2-up、Swipe、Onion Skin、Difference 模式按钮和两侧图片预览。

## 0.26.0.0 - 2026-05-26

- ✅[开发]-参考 Vex 新增 `ThemeOption`、`IThemeService` 和 `ThemeService`，封装 Semi 主题列表和 `Application.RequestedThemeVariant` 切换。
- ✅[开发]-查看菜单新增主题单选项，支持 system、light、dark、aquatic、desert、dusk、night-sky，并通过 ViewModel 命令切换。
- 🧩[开发]-`AppSettingsStore` 新增 `ThemeKey` 读写，`App.config` 默认写入 `ThemeKey=system`。
- 🌐[开发]-补充主题菜单、主题名称和主题切换状态提示的中英文资源，并同步 `AvaGithubDesktopL` 常量。

## 0.25.0.0 - 2026-05-26

- 🎨[开发]-新增 `UnifiedDiffViewer`、`UnifiedDiffLinePresenter` 和统一 diff 解析器，将原右侧只读 TextBox 替换为带行号、区块、增删行分色的 GitHub Desktop 风格差异视图。
- 🎨[开发]-新增 `OperationLogView`，继续消费 `CodeWF.Log.Core.Logger` 的 UI 日志流，但日志级别和右键菜单文本改由本项目 i18n 资源控制。
- 🧩[开发]-新增 `AvaGithubDesktop.Controls` 控件实现与 `AvaGithubDesktop.Controls.Themes/Themes/Controls` 控件主题文件，主窗口只负责绑定和控件组合。
- 🌐[开发]-补充日志级别、复制、清空、查看日志等中英文资源，并同步 `AvaGithubDesktopL` 常量。
- 🧪[验证]-构建通过；控件工程、主题工程和主程序均无警告；UIAutomation 打开 AvaGithubDesktop 仓库后确认 Diff 列表生成 11 行，操作日志显示本地化内容。

## 0.24.0.0 - 2026-05-26

- ✅[开发]-查看菜单新增语言子菜单，简体中文与 English 使用 Radio 菜单项并通过 ViewModel 命令切换。
- ✅[开发]-`AppSettingsStore` 新增 `CultureName` 读写，启动阶段从 `App.config` 初始化 Lang.Avalonia 和 Semi/Ursa 语言资源。
- ✅[开发]-工具栏语言下拉和菜单语言切换共用 `SelectedLanguage`，保证两处状态同步并统一写入配置。
- 🧪[验证]-构建通过；版本号为 `0.24.0.0`；中英文 JSON 资源可解析；MVVM 事件扫描无命中；通过 AvaGithubDesktop 从查看菜单切换到 English，确认 `App.config` 写入 `CultureName=en-US`，重启后界面保持英文。

## 0.23.0.0 - 2026-05-26

- ✅[开发]-接入 `CodeWF.LogViewer.Avalonia` NuGet 包，在主窗口状态栏上方新增 180px 操作日志栏。
- ✅[开发]-新增 `AppSettingsStore`，通过 `CodeWF.Tools.Files` 读写 `App.config`，当前维护 `IsOperationLogVisible`。
- ✅[开发]-查看菜单新增操作日志显隐开关，状态栏消息同步写入 `CodeWF.Log.Core.Logger`，让关键 Git 操作进入日志栏。
- 🧪[验证]-构建通过；版本号为 `0.23.0.0`；中英文 JSON 资源可解析；MVVM 事件扫描无命中；通过 AvaGithubDesktop 确认日志栏默认显示、接收状态日志，菜单隐藏后写入 `App.config` 并在重启后保持隐藏。

## 0.22.0.0 - 2026-05-26

- ✅[开发]-参考 GitHub Desktop History 提交操作，新增选中提交在 GitHub 打开的命令和按钮。
- ✅[开发]-`RepositoryRemoteUrlHelper` 新增提交页 URL 生成方法，复用已有 GitHub 远程地址解析逻辑，避免 ViewModel 直接拼装远程地址。
- 🌐[开发]-补充“在 GitHub 上查看提交”和提交页打开结果的中英文资源，并同步 `AvaGithubDesktopL` 常量。
- 🧪[验证]-构建通过；版本号为 `0.22.0.0`；中英文 JSON 资源可解析；MVVM 事件扫描无命中；通过 AvaGithubDesktop 切到 History，确认“在 GitHub 上查看提交”按钮可见且启用。

## 0.21.2.0 - 2026-05-26

- 🎨[开发]-参考 GitHub Desktop `commit-details`、`commit-summary` 和 History 截图，将历史详情区从多卡片改为提交摘要、变更文件列和 Diff 的单一连续面板。
- 🎨[开发]-新增 `historySurface`、`historySummary`、`historyFileHeader`、`diffHeader` 等主题样式，继续放在 `AvaGithubDesktop.Controls.Themes`，窗口 XAML 只组合布局和绑定。
- 🧪[验证]-构建通过；版本号为 `0.21.2.0`；MVVM 事件扫描无命中；通过 AvaGithubDesktop 切到 History，确认右侧显示提交摘要、4 个变更文件列表和 Diff。

## 0.21.1.0 - 2026-05-26

- 🎨[开发]-Changes 右侧有变更状态移除指标卡和摘要卡，改为 `PanelAlt` 头部加只读 Diff 的单栏布局，仓库概览信息继续由左侧列表、工具栏和状态栏承担。
- 🧪[验证]-构建通过；通过 AvaGithubDesktop 加载本仓库未提交变更，确认右侧已只显示 Diff 面板和文件标题。

## 0.21.0.0 - 2026-05-26

- 🎨[开发]-参考 GitHub Desktop 运行窗口和 `app/styles`，将主工具栏改成 50px 深色分段布局，仓库选择器、分支选择器和同步按钮按 Desktop 宽度比例对齐。
- 🎨[开发]-补充 Toolbar、PanelAlt、Hover、Selection 等主题色资源，继续维护在 `AvaGithubDesktop.Controls.Themes`，主程序只引用资源键。
- 🎨[开发]-Changes/History 标签栏改为 29px Desktop tab，并增加蓝色底部选中线。
- 🧩[开发]-无本地变更时隐藏右侧指标卡和摘要卡，改为内容区居中的“工作区干净”空状态。
- 🐛[修复]-`IsLoading` 结束时补发 `HasNoChanges` 通知，避免仓库加载完成后空状态不刷新。
- 🧪[验证]-构建通过；通过 UIAutomation 对比 GitHub Desktop 与 AvaGithubDesktop，确认工具栏列宽、tab 尺寸和 clean repo 空状态已生效。

## 0.20.1.0 - 2026-05-26

- 🐛[修复]-AvaGithubDesktop 自提交验证时发现中文路径被 Git `core.quotePath` 转义后直接传入 `git add` 会导致 pathspec 不匹配，已补充 C-style 引号路径解码。
- 🧩[开发]-`GitRepositoryService` 在 Changes 状态和 History 文件列表中统一还原 Git 引号路径，提交、丢弃、复制路径和 Diff 查询复用真实 Git 路径。
- 🧪[验证]-重新构建通过；AvaGithubDesktop 变更列表已将中文文档路径显示为真实路径，并已使用软件完成提交与推送验证。

## 0.20.0.0 - 2026-05-26

- 🧭[开发]-按反馈接入 `CodeWF.Tools.Files` NuGet 包，使用 `AppConfigHelper` 从 `App.config` 读取 `GitHubOAuthClientId`。
- 🔐[开发]-账号登录从 PAT 表单改为 GitHub OAuth Device Flow，发起 `device/code` 后打开浏览器授权并轮询 `access_token`。
- 🧩[开发]-`SignInWindowViewModel` 承载 OAuth 发起、验证码展示、浏览器打开和授权轮询逻辑，窗口继续只做关闭桥接。
- 🌐[开发]-调整登录窗口中英文资源为浏览器授权和验证码文案，并同步更新 T4 生成的 `AvaGithubDesktopL` 常量文件。
- 🧪[验证]-NuGet restore 与构建通过；JSON 资源、MSBuild 版本变量、MVVM 事件扫描和 `App.config` 输出复制均已验证。
- 🧪[验证]-通过 AvaGithubDesktop 账号菜单实测 OAuth 登录窗口，点击浏览器登录后可生成 GitHub 设备验证码并进入等待授权状态。

## 0.19.0.0 - 2026-05-26

- 🎨[开发]-新增 `app-logo.svg` 源稿，并生成 `app-logo.png`、`app-logo.ico`，主窗口标题栏和应用图标统一使用新 Logo。
- 🧩[开发]-新增 `SignInWindowViewModel`、`DiscardChangesConfirmationWindowViewModel` 和通用 `DialogCloseRequestedEventArgs`，窗口按钮全部改为命令绑定。
- 🧠[开发]-`SignInWindow.axaml.cs` 与 `DiscardChangesConfirmationWindow.axaml.cs` 仅保留关闭窗口桥接，不再承载点击事件业务逻辑。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；JSON 资源、MSBuild 版本变量、MVVM 事件扫描和主窗口/登录窗口截图均已验证。

## 0.18.0.0 - 2026-05-26

- 🧭[开发]-参考 GitHub Desktop `Account`、`AccountsStore` 和 `SignInStore`，先落地 PAT 登录切片，避免复用 Desktop 专属 OAuth ClientID/Secret。
- 🧩[开发]-新增 `GitHubAccount`、`GitHubAccountService`、`IAccountDialogService` 和 `SignInWindow`，支持 GitHub.com/Enterprise 端点规范化、令牌校验、账户资料读取和本地账户保存。
- 🎨[开发]-主窗口顶栏新增账号状态按钮和已登录弹出层，账号相关样式维护在 `AvaGithubDesktop.Controls.Themes`。
- 🌐[开发]-新增账号登录、退出登录和状态提示相关中英文资源键，并同步更新 T4 生成的 `AvaGithubDesktopL` 常量文件。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；JSON 资源和 MSBuild 版本变量已验证。

## 0.17.0.0 - 2026-05-26

- 🧭[开发]-参考 GitHub Desktop Repository 菜单与仓库列表项菜单，补齐仓库级 Open in external editor。
- 🧩[开发]-`MainWindowViewModel` 新增当前仓库与仓库列表项外部编辑器打开回调，复用 `OpenRepositoryPathInExternalEditorAsync`。
- 🧩[开发]-`RepositoryListItemViewModel` 增加 `OpenInExternalEditorCommand`，列表项菜单命令继续自包含。
- 🌐[开发]-新增仓库外部编辑器打开状态相关中英文资源键，并同步更新 T4 生成的 `AvaGithubDesktopL` 常量文件。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；JSON 资源、MSBuild 版本变量、`git diff --check` 和界面截图均已验证。

## 0.16.0.0 - 2026-05-26

- 🧭[开发]-参考 GitHub Desktop History 文件项菜单，为提交文件列表补齐复制路径、外部编辑器打开和文件管理器显示入口。
- 🧩[开发]-新增 `GitCommitFileItemViewModel`，提交文件项通过包装模型持有右键命令，避免在 XAML 中跨 DataContext 绑定命令。
- 🧠[开发]-`MainWindowViewModel` 新增 `SelectedCommitFiles` 集合，切换提交时重建文件项 ViewModel 并保留可匹配的选中文件路径。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；JSON 资源、MSBuild 版本变量和 `git diff --check` 均已验证。

## 0.15.0.0 - 2026-05-26

- 🧭[开发]-为 History 选中提交详情新增 Copy SHA，复制完整提交哈希而不是短 SHA。
- 🧩[开发]-`MainWindowViewModel` 新增 `CopySelectedCommitShaCommand` 和 `CanCopySelectedCommitSha`，继续复用剪贴板服务。
- 🎨[开发]-新增 `Button.inlineAction` 样式，提交详情内的小操作按钮不直接在窗口里写样式。
- 🌐[开发]-新增复制提交 SHA 相关中英文资源键，并同步更新 T4 生成的 `AvaGithubDesktopL` 常量文件。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；JSON 资源、MSBuild 版本变量和 `git diff --check` 均已验证。

## 0.14.0.0 - 2026-05-26

- 🧭[开发]-参考 GitHub Desktop Changes 文件项菜单，新增 Open in external editor，并在删除文件上禁用该入口。
- 🧩[开发]-`RepositoryShellService` 新增 `OpenItemAsync`，Windows 使用 ShellExecute，macOS/Linux 分别走 `open` 与 `xdg-open`。
- 🧩[开发]-`GitChangeItemViewModel` 新增 `OpenInExternalEditorCommand` 和 `CanOpenInExternalEditor`，主窗口负责解析仓库内文件完整路径。
- 🌐[开发]-新增外部编辑器打开相关中英文资源键，并同步更新 T4 生成的 `AvaGithubDesktopL` 常量文件。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；JSON 资源、MSBuild 版本变量和 `git diff --check` 均已验证。

## 0.13.0.0 - 2026-05-26

- 🧭[开发]-参考 GitHub Desktop Changes 文件项菜单，新增 Copy file path，和 Copy relative path 保持同组展示。
- 🧩[开发]-`GitChangeItemViewModel` 新增 `CopyFullPathCommand`，主窗口回调负责将 Git 相对路径解析为仓库内完整路径。
- 🌐[开发]-新增复制文件完整路径相关中英文资源键，并同步更新 T4 生成的 `AvaGithubDesktopL` 常量文件。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；JSON 资源、MSBuild 版本变量和 `git diff --check` 均已验证。

## 0.12.0.0 - 2026-05-26

- 🧭[开发]-参考 GitHub Desktop Changes 列表背景右键菜单，新增 Discard all changes 和 Stash all changes 入口。
- 🧩[开发]-`MainWindowViewModel` 新增 `DiscardAllChangesCommand` 和 `CanDiscardChanges`，批量丢弃复用文件级确认与执行流程。
- 🧠[开发]-抽出 `DiscardChangesAsync`、状态消息格式化方法，让单文件和多文件 discard 行为保持一致。
- 🌐[开发]-新增批量丢弃菜单和数量状态相关中英文资源键，并同步更新 T4 生成的 `AvaGithubDesktopL` 常量文件。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；JSON 资源、MSBuild 版本变量和 `git diff --check` 均已验证。

## 0.11.0.0 - 2026-05-26

- 🧭[开发]-参考 GitHub Desktop `filter-changes-list` 和 `discard-changes-dialog`，为 Changes 文件项补齐 Discard changes 入口和确认流程。
- 🧩[开发]-新增 `IConfirmationDialogService`、`ConfirmationDialogService` 和 `DiscardChangesConfirmationWindow`，确认窗口继续使用 `CodeWFWindow`。
- 🧩[开发]-`GitRepositoryService` 新增 `DiscardChangesAsync`，已跟踪文件通过 `git restore --staged --worktree` 还原，未跟踪文件删除前校验仓库边界。
- 🎨[开发]-确认文件列表、危险按钮、警告文本等样式维护在 `AvaGithubDesktop.Controls.Themes`，主应用不承载样式细节。
- 🌐[开发]-新增丢弃更改确认、状态提示和右键菜单中英文资源键，并同步更新 T4 生成的 `AvaGithubDesktopL` 常量文件。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；JSON 资源、MSBuild 版本变量、临时仓库 discard 行为和 `git diff --check` 均已验证。

## 0.10.0.0 - 2026-05-26

- 🧭[开发]-参考 GitHub Desktop Changes 文件列表右键菜单，新增复制文件相对路径和在文件管理器中显示文件。
- 🧩[开发]-`GitChangeItemViewModel` 增加文件项级右键命令，通过主窗口回调复用剪贴板和文件管理器服务。
- 🧩[开发]-`RepositoryShellService` 新增 `ShowItemInFileManagerAsync`，Windows 下选择文件，macOS/Linux 下打开对应位置；文件缺失时回退到父目录。
- 🌐[开发]-新增 Changes 文件菜单相关中英文资源键，并同步更新 T4 生成的 `AvaGithubDesktopL` 常量文件。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；JSON 资源、MSBuild 版本变量和 `git diff --check` 均已验证。

## 0.9.0.0 - 2026-05-26

- 🧭[开发]-参考 GitHub Desktop 仓库列表右键菜单，补齐复制仓库名称、复制仓库路径和 View on GitHub 操作。
- 🧩[开发]-新增 `RepositoryRemoteUrlHelper`，统一将 GitHub HTTPS/SSH remote 转换为可打开的网页地址。
- 🧩[开发]-`RepositoryShellService` 新增浏览器打开 URL 和剪贴板写入能力，仓库外部交互继续集中在服务层。
- 🧱[开发]-按反馈优化 `Directory.Build.props`，新增 `AvaGithubDesktopVersion` 版本变量，避免 `Version`、`FileVersion` 等属性重复硬编码。
- 🌐[开发]-新增仓库复制和 GitHub 打开相关中英文资源键，并同步更新 T4 生成的 `AvaGithubDesktopL` 常量文件。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；JSON 资源、MSBuild 版本变量和 `git diff --check` 均已验证。

## 0.8.0.0 - 2026-05-26

- 🧭[开发]-参考 GitHub Desktop 仓库列表项右键菜单，为仓库选择器列表项补齐 Open、Open in terminal、Show in file manager。
- 🧩[开发]-`RepositoryListItemViewModel` 新增列表项级外部操作命令，通过委托复用主窗口的仓库打开与外部服务逻辑。
- 🧠[开发]-当前仓库菜单和仓库列表项统一走 `OpenRepositoryPathInShellAsync`、`ShowRepositoryPathInFileManagerAsync`，减少重复错误处理。
- 🌐[开发]-调整仓库外部操作状态文案为通用仓库描述，兼容当前仓库和列表项仓库两种来源。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；JSON 资源、MSBuild 版本属性和 `git diff --check` 均已验证。

## 0.7.0.0 - 2026-05-26

- 🧭[开发]-参考 GitHub Desktop Repository 菜单新增当前仓库“在终端中打开”和“在文件管理器中显示”入口。
- 🧩[开发]-新增 `IRepositoryShellService`、`RepositoryShellService`，集中处理 Windows、macOS、Linux 的文件管理器与终端启动。
- 🧠[开发]-`MainWindowViewModel` 新增两个当前仓库命令，命令启用状态跟随 `HasRepository` 和仓库操作忙闲状态。
- 🌐[开发]-新增仓库外部操作相关中英文资源键，并同步更新 T4 生成的 `AvaGithubDesktopL` 常量文件。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；JSON 资源、MSBuild 版本属性和 `git diff --check` 均已验证。

## 0.6.0.0 - 2026-05-26

- 🧭[开发]-参考 GitHub Desktop `repositories-list` 实现顶部 Current repository 弹出选择器，包含 Filter、Add 和分组仓库列表。
- 💾[开发]-新增 `RepositoryHistoryService`，持久化用户打开过的仓库，并在开发环境扫描常用仓库目录作为初始列表。
- 🧩[开发]-新增 `RepositoryListGroupViewModel`、`RepositoryListItemViewModel`，让仓库列表项自身持有打开命令，简化嵌套模板绑定。
- 🎨[开发]-新增仓库选择器、仓库过滤、仓库列表项和仓库弹出层样式，继续维护在 `AvaGithubDesktop.Controls.Themes`。
- 🌐[开发]-新增仓库选择器相关中英文资源键，并同步更新 T4 生成的 `AvaGithubDesktopL` 常量文件。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；JSON 资源、MSBuild 版本属性、`git diff --check` 和仓库选择器 UIAutomation 弹出层验证均已通过。

## 0.5.0.0 - 2026-05-26

- 📖[开发]-参考 Vex 新增 `MarkdownDocumentWindow`，使用 `CodeWF.Markdown` 的 `MarkdownViewer` 展示 Markdown 文档。
- 🧭[开发]-新增 `IHelpService`、`HelpService`，Help 菜单补齐更新日志和关于入口。
- 🧩[开发]-关于窗口从程序集读取当前版本号，确保 `Directory.Build.props` 中的版本能展示到界面。
- 🌐[开发]-新增 Help/关于相关中英文资源键，并同步更新 T4 生成的 `AvaGithubDesktopL` 常量文件。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；JSON 资源、MSBuild 版本属性和更新日志输出复制均已验证。

## 0.4.0.0 - 2026-05-26

- 📦[开发]-新增 `GitStashEntry` 模型，仓库快照增加 `CurrentBranchStash`，Git 服务支持创建、恢复、丢弃 Desktop marker stash。
- 🧭[开发]-Changes 面板新增 `Stash all changes` 按钮和 `Stashed Changes` 区块，提供 Restore、Discard 操作；Branch 菜单补齐对应入口。
- 🧠[开发]-ViewModel 新增 stash 操作状态机，提交、同步、切分支、stash 操作之间互斥，避免并发 Git 操作互相覆盖工作区。
- 🌐[开发]-新增 stash 相关中英文资源键，并同步更新 T4 生成的 `AvaGithubDesktopL` 常量文件。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；JSON 资源校验通过；临时 Git 仓库覆盖 stash、restore、drop；已生成 `docs/screenshots/stash-actions.png`。

## 0.3.0.0 - 2026-05-26

- 🌿[开发]-将顶部普通分支 `ComboBox` 调整为 Desktop 风格 Current Branch 弹出选择器，弹出层包含分支过滤、数量提示、当前分支标记和切换按钮。
- 🔎[开发]-新增 `FilteredBranches`、`BranchFilterText`、分支过滤计数和空结果状态；过滤仅影响弹出层列表，不改动真实分支集合。
- 🎨[开发]-补充 Branch 选择器、分支过滤输入框、弹出层容器和分支列表样式，样式继续维护在 Controls.Themes 工程。
- 🌐[开发]-新增分支选择器相关中英文资源键，并同步更新 T4 生成的 `AvaGithubDesktopL` 常量文件。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；JSON 资源校验通过；已生成 `docs/screenshots/branch-selector.png` 进行界面验证。

## 0.2.0.0 - 2026-05-26

- 🧩[开发]-参考 Vex 将版本号和产品元数据集中到根目录 `Directory.Build.props`，当前软件版本迭代为 `0.2.0.0`。
- 🧩[开发]-新增 `docs/更新日志.md` 和 `docs/开发日志.md`；更新日志随应用复制到输出目录，供后续 `CodeWF.Markdown` 更新日志窗口加载。
- 🔎[开发]-实现 Changes 文件过滤：新增过滤文本状态、过滤集合、过滤后全选范围和过滤命中文件计数，界面补齐 Desktop 风格 Filter 输入框。
- 🌐[开发]-完善本轮过滤功能的中英文资源键，并同步更新 T4 生成的 `AvaGithubDesktopL` 常量文件。
- 📝[开发]-补充 Git 同步、提交选择、Changes 过滤和快照刷新路径的中文维护注释，说明关键状态联动和设计边界。
- 🧪[验证]-构建 `AvaGithubDesktop.slnx` 通过；JSON 资源、MSBuild 版本属性、更新日志输出复制、`git diff --check` 和 Changes 过滤界面截图均已验证。

## 0.1.0.0 - 2026-05-26

- ✅[开发]-已完成仓库打开与 Git 状态概览、Changes 提交面板、History 提交列表、选中文件 Diff 预览、本地分支 Checkout、主题工程拆分、T4 语言资源和 Fetch/Pull/Push 远端同步入口。
